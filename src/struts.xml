<?xml version="1.0" encoding="UTF-8" ?>  
 <!DOCTYPE struts PUBLIC  
"-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"  
"http://struts.apache.org/dtds/struts-2.5.dtd">  
<struts>  
    <!-- 该属性指定需要Struts2处理的请求后缀，该属性的默认值是action，即所有匹配*.action的请求都由Struts2处理。
        如果用户需要指定多个请求后缀，则多个后缀之间以英文逗号（,）隔开。
    <constant name="struts.action.extension" value="do" /> -->
    <!-- 设置浏览器是否缓存静态内容,默认值为true(生产环境下使用),开发阶段最好关闭 -->
    <constant name="struts.serve.static.browserCache" value="false" />
    <!-- 当struts的配置文件修改后,系统是否自动重新加载该文件,默认值为false(生产环境下使用),开发阶段最好打开 -->
    <constant name="struts.configuration.xml.reload" value="true" />
    <!-- 开发模式下使用,这样可以打印出更详细的错误信息 -->
    <constant name="struts.devMode" value="true" />
    <!-- 默认的视图主题 -->
    <!-- <constant name="struts.ui.theme" value="simple" /> -->
    <!-- <constant name="struts.objectFactory" value="spring" />  -->
    
  <package name="news" extends="json-default">  
    <action name="addNews" class="com.zzy.action.NewsAction" method="addNews">  
    	<result name="addnews" type="json">
                <!-- 此处将reslut的值返回给客户端，root的值对应要返回的值的属性result
                      注意：root为固定写法，否则不会把result的值返回给客户端 -->
                <param name="root">message</param>
       	</result>
    </action>
    <action name="listNews" class="com.zzy.action.NewsAction" method="listNews">
    	<result name="pageJson" type="json">
    		<param name="root">pageJson</param>
    </result>
    </action>
    <!-- 页面跳转 -->
    <action name="goUpdate" class="com.zzy.action.NewsAction" method="goUpdate">
    	<result name="go_update">/NewsPage/updateNews.jsp</result>
    </action>
    <action name="updateNews" class="com.zzy.action.NewsAction" method="updateNews">
    	<result type="json" name="update_success">
			<param name="root">message</param>
		</result>
    </action>
    <action name="delNews" class="com.zzy.action.NewsAction" method="delNews">
    	<result type="json" name="delNews">
    		<param name="root">status</param>
    	</result>
    </action>
  </package>  
  
  <package name="category" extends="json-default">
  <!-- 添加类型 -->
  	<action name="addCategory" class="com.zzy.action.NewsAction" method="addCategory">
  		<result name="addCategory" type="json">
  			<param name="root">message</param>
  		</result>
  	</action>
  <!-- listCategory -->
  	<action name="listCategory" class="com.zzy.action.NewsAction" method="listCategory">
  		<result name="listCategory" type="json">
  			<param name="root">pageJson</param>
  		</result>
  	</action>
  	<!-- 页面跳转 -->
    <action name="goUpdateC" class="com.zzy.action.NewsAction" method="goUpdateC">
    	<result name="go_update">/CategoryPage/updateCategory.jsp</result>
    </action>
    <action name="updateCategory" class="com.zzy.action.NewsAction" method="updateCategory">
    	<result type="json" name="update_success">
			<param name="root">message</param>
		</result>
    </action>
    <action name="delCategory" class="com.zzy.action.NewsAction" method="delCategory">
    	<result type="json" name="delNews">
    		<param name="root">status</param>
    	</result>
    </action>
  </package>
</struts>